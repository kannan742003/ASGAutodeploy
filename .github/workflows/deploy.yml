name: Deploy to AWS CodeDeploy

on:
  push:
    branches:
      - main
      
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up JDK 11
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'

      - name: Build with Maven
        run: mvn clean package

      - name: Verify WAR file exists
        run: |
          if [ ! -f "target/MyApp-1.0-SNAPSHOT.war" ]; then
            echo "WAR file not found in target directory!"
            exit 1
          fi

      - name: Prepare deployment package
        run: |
          mkdir -p deploy/scripts
          cp target/MyApp-1.0-SNAPSHOT.war deploy/
          cp scripts/before-install.sh deploy/scripts/
          cp scripts/after-install.sh deploy/scripts/
          cp scripts/application-start.sh deploy/scripts/
          cp appspec.yml deploy/
          cd deploy
          zip -r MyApp.zip .
          ls -lh MyApp.zip # List the zip file to confirm its creation

      - name: Debug: List files in deploy directory
        run: ls -l deploy

      - name: Upload to S3
        env:
          AWS_REGION: us-west-2
          S3_BUCKET: asgautodeploy  
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          ls -lh deploy/MyApp.zip # Ensure the zip file exists before upload
          aws s3 cp deploy/MyApp.zip s3://$S3_BUCKET/deployments/MyApp.zip --region $AWS_REGION

      - name: Deploy to AWS CodeDeploy
        env:
          AWS_REGION: us-west-2
          CODEDEPLOY_APP_NAME: javaapp
          CODEDEPLOY_DEPLOYMENT_GROUP: Asgdeploymentgrp
          S3_BUCKET: asgautodeploy  
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          aws deploy create-deployment \
            --application-name $CODEDEPLOY_APP_NAME \
            --deployment-group-name $CODEDEPLOY_DEPLOYMENT_GROUP \
            --s3-location bucket=$S3_BUCKET,key=deployments/MyApp.zip,bundleType=zip \
            --region $AWS_REGION
